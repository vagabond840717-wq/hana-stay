# HANA STAY — 작업 현황

## 마지막 작업
- **날짜**: 2026-09-05
- **한 것**: 부킹·트립 앞잘림 복원 + 오늘 퇴실 되살리기 ([#29](docs/05-known-issues.md))

### 배포 완료 ✅ (2026-09-04 ~ 09-05)
| 저장소 | 커밋 | 내용 |
|---|---|---|
| `ical-proxy` | `6448401` | 앞잘림 시작일 복원 (`untrimSegs`) + `fix=bk` 추가 |
| `ical-proxy` | `866430a` | 오늘 퇴실 되살리기 (`restoreTodayCheckouts`) |
| `jnjhana` (청소앱) | `6b5cbf6` | `fetchBkIcal` 에 `&fix=bk&room=` (1줄) |
| `booking` (예약앱) | — | **변경 없음** |

되돌아갈 지점: 워커 `2444942` / 청소앱 `696dfa0`

**발단** — "어제 체크인해서 들어온 방인데 오늘 또 체크인으로 보인다".
601호 부킹 `9/3~9/5` 예약이 9/4 엔 `9/4~9/5` 로 앞이 잘려 오고, 9/5 엔 **통째로 사라졌다.**
9/5 는 부킹 퇴실 + 에어비앤비 입실이 겹친 **같은 날 교대**인데 화면엔 `인` 만 떴다.

**밝혀진 것** — 부킹닷컴도 트립처럼 앞을 자른다. 자르는 기준은 채널이 뭘 내보내느냐다:
`Reserved`(에어비앤비, 예약) → 안 자름 / `CLOSED - Not available`·`RoomStatus Fully booked`(재고) → 자름.
[DRAFT-past-ledger.md](docs/features/DRAFT-past-ledger.md) 의 "트립에서만 관측된다"는 서술을 정정했다.

검증:
- 실데이터 26 PASS / 0 FAIL (앞잘림) + 21 PASS / 0 FAIL (오늘 퇴실)
- **"오늘 이후 시작하는 조각이 새로 생겼나" 전수 = 0건** → 판매 가능일 무변화 증명
- 601호 아카이브에 어제 꼬리 `20270904~20280304` 가 있는데도 **안 건드림** 확인 (꼬리 오판 방지)
- 배포 후 두 앱 실측 일치: 601호 `9/3 인 · 9/4 숙박중 · 9/5 아웃/인`, 402호 `9/2 인`
- 내보내기에 되살린 조각이 안 들어감 확인

---

## 그 전 작업
- **날짜**: 2026-09-02
- **기기**: (이 세션)
- **한 것**: 트립 '당일 객실 마감' 가짜 하룻밤 걷어내기 — A·B·C 배포 완료
  - 스펙: [DRAFT-trip-daylock.md](docs/features/DRAFT-trip-daylock.md) / 계획: [PLAN-trip-daylock.md](docs/features/PLAN-trip-daylock.md)

### 배포 완료 ✅ (2026-09-02)
| 저장소 | 커밋 | 내용 |
|---|---|---|
| `ical-proxy` (워커) | `f2d71c9` | 가짜 하룻밤 판정(`tr_daylock`) + 장부·`/bookings`·`fix=tr` 교정 + 소급 청소 |
| `jnjhana` (청소앱) | `696dfa0` | `fetchTrIcal`에 `&fix=tr&room=` 옵트인 (1줄) |
| `booking` (예약앱) | — | **변경 없음** (`/bookings`를 읽어 자동 반영) |

되돌아갈 지점: 워커 `dcd622b` / 청소앱 `39d6f91`

**발단** — 402호 트립 예약이 `9/2~9/11`인데 예약앱이 **9/1 체크인**으로 표시(01:11 발견).
앱 버그 아님. 트립이 `DTSTART:20260901`로 보냈다. [#28](docs/05-known-issues.md)의 규칙인데
문서엔 **퇴실 쪽만** 적혀 있었다 — 이번은 **입실 쪽**, 같은 규칙의 반대편.

**진짜 피해는 장부였다** — 아카이브의 "긴 쪽 남기기"(앞잘림 방어 = 정답)가, 가짜는 항상 길어서
**매번 가짜를 골랐다.** 8/27 07:00에 트립이 `8/27~8/30`으로 고쳐 보냈는데 장부가 거절하고
`8/26~8/30`(가짜)을 지켰다. 8/25 재점화 이후 5건 오염. 화면은 07시에 낫지만 장부는 안 낫는다.

검증:
- 실데이터 단위시험 **35 PASS / 0 FAIL** (네 모양 뺄셈 · 자물쇠 ①② 거부 · 자정 넘김 · KV write 생략 · ICS 수술)
- **`8/26~8/30` − 8/26밤 = `8/27~8/30`** — 트립이 다음날 07:00:55에 실제로 보낸 값과 일치
- 배포 전후 **`/ical/` 45개 주소 바이트 동일** → 다른 채널 무영향 (오버부킹 위험 0)
- 소급 청소 5건 적용, 다른 8개 호실·402호 다른 플랫폼 **무변경**
- 배포 후 두 앱 실측: 402호 **9/1 빈칸 / 9/2 인 / 9/11 아웃**, 둘 다 일치
- 07:00 해제 실측 확인 — 피드가 `20260901` → `20260902`로 바뀜

**사장님 조치 (2026-09-02)**: 트립 `예약 마감 시간`을 전 호실 **12:00**으로 변경.
당일 예약보다 오버부킹이 더 문제라는 판단. 부수 효과로 **17:30 이후 진짜 신규 예약이 불가능해져
가짜 판정이 더 안전해진다.** 되돌리지 말 것.

### D단계도 배포 완료 ✅ (2026-09-03)
| 저장소 | 커밋 | 내용 |
|---|---|---|
| `ical-proxy` (워커) | `2444942` | `/ical/<호실>/ab` 에서만 가짜 하룻밤 제거 |

되돌아갈 지점: `f2d71c9`

**범위를 사용자가 좁혔다** — "부킹 리브도 필요없어 당일은 에어비앤비만 팔아야 될 것 같아".
이게 원안(세 채널 다 열기)보다 **안전하다**: 그 밤을 팔 수 있는 채널이 하나뿐이라
**두 채널이 같은 밤을 동시에 파는 경쟁이 없다.**

**전제 작업 (사장님, 2026-09-03)**: 에어비앤비 9개 방 구독 주소를 옛 통합 주소에서
`/ical/<호실>/ab` 로 교체. 교체 전에는 에어비앤비만 여는 것이 **불가능**했다 —
옛 주소는 부킹·리브도 같이 보는 공용 문서다.
- 실측으로 확인: 옛 주소와 `/ab` 의 차이는 **에어비앤비 자기 예약뿐**(503호 2건). 잃는 보호 없음
- 부킹닷컴은 이미 교체돼 있었다 (8/6에 사라졌던 603호 `9/8~9/12` 예약이 살아 있음)

검증:
- `exportIcal` 을 실피드로 돌려 **17 PASS / 0 FAIL**
- 가짜 있을 때: `/ab` 만 그 밤이 열리고 **레거시·`/bk`·`/lv` 는 막힌 채 유지**
- `daylock` 읽기 실패 시 원본 그대로(막은 채) — 안전한 방향
- 수동 블락은 별도 VEVENT라 뺄셈이 안 닿음 → **사용자 블락이 최종 결정권**
- 배포 후 `/ab` 응답이 현재 피드와 **한 밤도 다르지 않음** 확인

### ⏳ 필터는 아직 한 번도 작동한 적이 없다
402호에 **9/11까지 손님이 있어** 그날 밤들이 이미 덮여 있다 → 트립이 18시에 잠가도
새 조각이 안 생겨 알림 로그에 `18:00` 이벤트 자체가 안 뜬다 (9/2~9/4 실측 0건).
**601·603은 이 규칙이 안 걸려 있다.**
⇒ **첫 실전 작동은 402호가 비는 9/11 이후**다. 그때 아래 항목으로 확인할 것.

---

## 그 전 작업
- **날짜**: 2026-08-09
- **기기**: (이 세션)
- **한 것**: 체크아웃 날 수동 블락 버튼 — 유실분 재구현 후 배포 (얼리체크인 대응)
  - 계획서: [PLAN-checkout-day-block.md](docs/features/PLAN-checkout-day-block.md) Task 1·6

### 배포 완료 ✅ (2026-08-09)
| 저장소 | 커밋 | 내용 |
|---|---|---|
| `booking` (예약앱) | `7035538` | 체크아웃만 있는 날에도 수동 블락 폼 표시 + 반반 셀 오른쪽 글자 `인`→`블락` |
| `booking` (예약앱) | `46a5d89` | 경계 자동 은퇴 폐지 — 사용자가 해지할 때까지 유지 ([#27](docs/05-known-issues.md)) |
| `booking` (예약앱) | `80e2e38` | 블락·경계 저장 시 서버 최신본 병합 — 남의 기록 덮어쓰기 차단 ([#22](docs/05-known-issues.md), [#26](docs/05-known-issues.md)) |

되돌아갈 지점: 예약앱 `7eb5e47`

**경계 자동 은퇴 폐지 (`46a5d89`)** — 사용자 질문("내가 해지할 때까지 남는 거 아니었어?")에서 출발해 발견.
`retireCuts()`가 트립 피드 흔들림(#25)에 반응해 **사용자가 손으로 만든 경계를 KV에서 영구 삭제**하고 있었다.
- 재현 성공: 402 jnj 피드에서 8/8~8/12만 빼자 옛 코드가 8/10 경계 삭제 → 수정 후 잔존 확인
- 화면 영향 0 (현재 실피드로는 옛 코드도 지울 게 없음 → `changed=false`), KV 값 시험 전후 동일
- 부수 효과: `loadBookingsFromKV`의 `saveCuts()`가 사라져 **자동 덮어쓰기 경로**도 함께 막힘
- 조사 중 발견한 별건 → [#26](docs/05-known-issues.md) 신규 등록 (경계 로드 실패 시 화면에서 사라져 보임 + 그 상태에서 편집하면 나머지 유실)

**왜 다시 했나** — 이 작업(Task 1·6)은 2026-08-05에 구현·검증까지 끝냈지만 푸시 전에
아래 "⚠ 사고"로 유실됐다. 청소앱 쪽(Task 2~5)은 그때 푸시돼서 살아 있었고, 예약앱만 날아가
"청소앱은 표시 준비 완료 / 예약앱엔 만드는 버튼 없음" 상태로 두 달 가까이 방치돼 있었다.

검증(배포 전, 배포본에 수정 함수만 주입 → 실데이터):
- 1,935칸 전수 대조 — 블락 폼 **새로 생긴 칸 49개**(전부 체크아웃만 있는 날) / **없어진 칸 0개**
- `ab-block` 가드가 오판 2칸 차단 (501 hana 8/12, 503 hana 12/18 — 에어비앤비가 이미 막은 밤)
- 케이스 10건 통과: 아웃만 ✅뜸 / 아웃2건 ✅뜸 / 빈날 ✅뜸 / 체크인·숙박중·🚫블락 ❌안뜸
- 셀 덤프 대조: 1,935칸 중 **1칸만 변경** (402 jnj 7/4 라벨 `인`→`블락`, 클래스 동일)
- 배포 후 실사이트에서 401 hana 8/12 열어 버튼·기본날짜(8/12~8/12) 확인

### 남은 일 — `/done` 미실행
[PLAN-checkout-day-block.md](docs/features/PLAN-checkout-day-block.md) Task 7이 아직 안 됨:
`05-known-issues.md` #9 보강, `04-apps-spec.md` 반영, `DRAFT-checkout-day-block.md` → 확정 rename

---

## 그 전 작업 (2026-08-06)
- **한 것**: 트립 예약이 앱에서 안 사라지는 문제 조사 → 버그 2건 수정·배포 + 문서 갱신
  - 계획서: [PLAN-cuts-sync-fix.md](docs/features/PLAN-cuts-sync-fix.md)

### 배포 완료 ✅
| 저장소 | 커밋 | 내용 |
|---|---|---|
| `jnjhana` (청소앱) | `39d6f91` | 경계 키 `tr_splits` → `tr_cuts` — 경계가 전혀 반영 안 되던 문제 ([#24](docs/05-known-issues.md)) |
| `booking` (예약앱) | `7eb5e47` | `loadBookingsFromKV`에서 `_raw_` 백업 폐기 — ↻ 눌러도 취소 예약이 안 사라지던 문제 ([#23](docs/05-known-issues.md)) |

되돌아갈 지점: 청소앱 `4daea6f` / 예약앱 `d3876ed` (둘 다 2026-08-06 02:33)

검증(배포 전, 실데이터): 배포본에 수정 함수만 주입 → 전체 셀 클래스 덤프 대조
- 청소앱 1,656칸 중 **1칸만** 변경 (402호 8/10 `occupied` → `both`)
- 예약앱 1,935칸 중 **0칸** 변경 (유령 데이터가 있을 때만 걷어냄)
- 유령 재현 테스트: `_raw_`에 8/6 예약 심고 ↻ → 되살아남(버그 확인) → 수정 후 즉시 사라짐
- 경계 편집(추가/삭제) 즉시 반영 확인, KV `tr_cuts` 값 원상복구 확인

### 조사에서 밝혀진 것 (버그 아님)
- 트립닷컴은 **문의 단계**에서 이미 날짜를 iCal로 내보내고, 거절해도 **30분 이상, 날짜별로 순차 해제**된다 ([#25](docs/05-known-issues.md))
- 트립 iCal은 실제 예약과 수동 방닫기를 **구분해 주지 않는다** (둘 다 `RoomStatus Fully booked`)
- 트립 iCal에는 가져온 남의 달력이 되비치지 않는다 (실측 확인)

### ⚠ 사고 — 다른 세션의 미배포 수정 유실
`E:\airbnb\app\JnJ booking\index.html` 을 검증본으로 덮어썼는데, 그 파일에 **직전 세션의 미배포 수정**
(수동 블락이 다른 기기 변경을 덮어쓰던 버그 → `saveBlocksMerged`, [#22](docs/05-known-issues.md))가 들어 있었다.
`app/` 은 `.gitignore` 대상이라 이력이 없어 **파일 자체는 복구 불가.**

다만:
- 그 수정은 **구버전 파일**(`tr_splits` 시대, 155,949 bytes)에 적용돼 있었다 → 그대로는 어차피 배포 불가였다
- 수정 내용은 세션 기록(`local_dee97901…` "예약앱 과거 날짜 기록 유지")에 남아 있어 **재작성 가능**
- 현재 배포본에 `saveBlocksMerged` 없음 확인 → **#22 버그는 아직 살아 있다**
- **추가 확인 (2026-08-09)**: 이 사고로 유실된 건 #22 하나가 아니었다. `checkout-day-block`
  Task 1·6도 같이 날아가 있었다 → 2026-08-09 재구현·배포 완료(`7035538`).
  **다른 미배포 유실분이 더 있는지는 여전히 미확인** — 배포본과 계획서를 대조하는 식으로 찾아야 한다

**저장 병합 (`80e2e38`)** — #22(수동 블락)·#26(경계)이 같은 뿌리라 한 건으로 묶어 처리.
`saveListMerged` 하나로 통일: 저장 직전 서버 최신 배열 재수신 → 내 변경만 얹기, **서버 못 읽으면 저장 안 함**.
- 검증: POST 가로채기로 payload 확인(서버 무변경) + 실서버 왕복(12/25 추가→삭제→원상복구)
- 부작용 없음: 콘솔 에러 0, 달력 1,935칸 정상. 시험 후 KV `tr_cuts`·`manual_blocks` 원래 값 확인

## 다음에 할 것
- [ ] **아카이브 꼬리 누적 정리** — 판매 오픈 기간 꼬리가 양끝이 매일 굴러가
      `09-01~03-01`, `09-02~03-02` … 처럼 **하루 한 줄씩 영구히 쌓인다**.
      포함 관계가 아니라 '부분 스냅샷 제거'에 안 걸린다. 화면엔 무해하나 용량이 계속 는다
- [ ] **⭐ 9/11 이후 첫 실전 작동 확인** — 402호가 비는 밤에 트립이 18시에 잠그면:
      1. `GET /push/events` 에 `18:00:5x` 402 jnj 트립 이벤트가 뜬다
      2. `GET /daylock` 의 `fake` 에 `402 jnj` 가 잡힌다 (`night` 가 그날)
      3. `GET /bookings` · 청소앱 화면에서 그 밤이 빠진다 → **두 앱 표시가 같은지 확인**
      4. `GET /archive` 에 가짜가 **안 쌓인다**
      5. `GET /ical/402 jnj/ab` 에서만 그 밤이 열리고, 레거시·`/bk`·`/lv` 는 막힌 채다
      → 하나라도 어긋나면 워커 `f2d71c9`(D 이전) 또는 `dcd622b`(전체 이전)로 revert
- [ ] **리브애니웨어 구독 주소 확인 (사장님)** — `/lv` 인지. 옛 주소면 부킹 때와 같은 사고
      (자기 예약이 자기 달력에서 사라짐, #26) 위험이 남아 있다
- [ ] **`/done trip-daylock`** — 스펙 확정 (`DRAFT-` 제거)
- [ ] **미배포 past-ledger 처리** — `E:\airbnb\ical-proxy\worker.js` 에 2026-08-15 작성한 258줄이
      커밋 없이 남아 있다. 이번 작업과 무관하며 **깨끗한 clone 에서 별도로** 진행할 것
- [ ] **`/done checkout-day-block`** — 문서 확정 (Task 7)
- [ ] **실전 확인 (사용자)**: 여러 기기에서 블락 추가/해제해보며 서로 지우지 않는지 (`80e2e38` 이후)
- [x] ~~청소앱도 같은 점검~~ — 확인 완료(2026-08-10): 청소앱은 `manual_blocks`를 **GET만** 한다(L931).
      경계(`tr_cuts`)도 읽기 전용. **쓰는 쪽은 예약앱 하나뿐**이라 병합 이식 불필요
- [ ] 사장님 기기에서 두 앱 **F5 한 번** (브라우저가 옛 파일을 캐시함 — 그다음부턴 ↻ 로 충분)
- [ ] 집 컴퓨터에 Claude Code 설치 + 저장소 연결
- [ ] 완료된 계획서(`PLAN-*.md`)의 Task를 배포본과 대조 — 유실분이 더 있는지 확인

## 진행 중인 기능
- 없음

## 미완성 / 보류 중
- `docs/features/DRAFT-cleaning-stats.md` — 청소 통계 기능 스펙 작성 중
- `docs/features/booking-split.md`, `split-inherit.md` — 상단에 구버전 경고만 달아둠. 본문 전면 개정은 미착수

---
## 작업 규칙 (2026-08-06 확정)
- **앱 소스 원본은 깃허브.** `E:\airbnb\app/` 는 "마지막으로 검증된 정상본" 백업일 뿐 — 읽기 참조 금지
- 코드 작업은 항상 `git clone --depth 1 <저장소>` 부터
- **덮어쓰기 전 그 파일에 미반영 작업이 없는지 확인할 것** (위 사고의 교훈)
- 푸시 = 즉시 배포. 검증을 배포 전에 끝낸다

---
> 새 세션 시작 시 Claude에게: "STATUS.md 읽고 어디까지 했는지 알려줘"
