# 📌 지금 할 일

## 09/24 예약 메모 표시

메모가 있는 예약의 달력 칸마다 오른쪽 위에 📝 를 붙인다. **스펙 초안 완료 — 사장님 승인 대기. 아직 코딩 전.**

- 스펙: [`DRAFT-memo-indicator.md`](features/DRAFT-memo-indicator.md) — 결정 8개 확정 (청소앱은 메모 계속 보이게)
- 남은 질문 1개: 부킹·트립·리브 메모가 청소앱에 안 보이는 문제를 이번에 같이 고칠지
- 승인 나면: `/plan memo-indicator`

<details><summary>자세히</summary>

- 조사 결과 (예약앱 `c21b0d9` / 청소앱 `6b5cbf6` / 워커 `9709111` 기준)
  - 메모는 이미 서버(`extra_<호실|입실일>`)에 저장된다. 휴대폰에도 사본이 있다
  - 비밀번호와 메모가 **같은 칸**에 들어 있다 → 청소앱이 메모도 본다
  - 예약앱은 채널 표시 없이 `호실|입실일`, 청소앱은 부킹·트립·리브에 `_bk/_tr/_lv` 를 붙인다 → 에어비앤비 외엔 두 앱이 서로 다른 칸을 본다
  - 반반 칸에서는 예약앱이 **맨 앞 예약 하나**의 칸에만 메모를 적는다 (나가는 손님에게 붙을 수 있음)
  - 워커가 iCal 고유번호(UID)를 **버린다** → 고유번호 연결에는 워커 수정이 필요
- 기존 '09/24 트립 블락 표시' 는 아래 🧊 로 미뤘다
</details>

---

# 📖 지나온 자리

> 사장님이 **"9월 23일 그거 이어서 해"** 처럼 말할 수 있게, 날짜 + 쉬운 이름으로 적는다.
> **세션 제목도 같은 이름**으로 맞춘다 — 사이드바에서 바로 찾을 수 있게.
> 형식: `09/24 트립 블락 표시` / 며칠 걸리면 `09/22~24 유령 체크아웃` (뒤 날짜 = 마지막으로 손댄 날)

**이 세션** — `09/22~24 유령 체크아웃`

| 이름 | 무슨 일이었나 | 결과 |
|---|---|---|
| `09/24 예약 메모 표시` | 메모 있는 예약에 📝 표시 | ⏳ 지금 여기 (확인 대기) |
| `09/24 트립 블락 표시` | 블락에 🌐 트립 표시 달기 | 🧊 미룸 — 스펙 `DRAFT-manual-trip-booking.md`, 시작: `/plan manual-trip-booking` |
| `09/23 수첩이 진짜 예약 지움` | 진짜 예약이 저장되자마자 지워짐 | ✅ 12건 복구 · [#34](05-known-issues.md) |
| `09/23 입실일 밀림` | 투숙 중 손님이 오늘 체크인으로 보임 | ✅ 원장에서 복원 · [#35](05-known-issues.md) |
| `09/22 603호 유령 체크아웃` | 20일에 나간 손님이 22일에 또 나감 | ✅ 멈춰 있던 원장 되살림 · [#33](05-known-issues.md) |

**커밋**

| 무엇 | 커밋 | 되돌아갈 지점 |
|---|---|---|
| 원장 복구 + 피드 변화 기록 | 워커 `9ea302c` | `d006986` |
| 수첩 정리 규칙 축소 + 입실일 복원 | 워커 `9709111` | `9ea302c` |
| 예약앱 같은 규칙 반영 | 예약앱 `c21b0d9` | `96361be` |

---

# 🧊 미뤄둔 것

| 이름 | 언제 할 수 있나 |
|---|---|
| **최신이 이긴다** — 수첩 줄마다 '마지막 본 시각'을 적고 충돌 시 최신을 고른다 | 변화 기록이 며칠 쌓인 뒤. 지금 하면 **긴 차단 덩어리가 '최신'으로 이겨서** 진짜를 또 덮는다 |
| **월별 결산** — 청소횟수·예약율 1년치 | 원장이 한 달 찬 뒤 (10월 하순) |
| **지난 화면도 원장으로** — 과거 달력이 아직 수첩을 본다 | 원장이 한 달 찬 뒤 |
| **리브애니웨어 403** — 워커가 피드를 못 받음 (203·401호) | 사장님이 요청할 때. 지금 예약 0건이라 피해 없으나 **유일한 실제 오버부킹 경로** |

---

# 🚫 다른 세션이 하는 일 — 건드리지 말 것

- **비품앱** — `docs/features/DRAFT-supply-store.md` · `PLAN-supply-store.md` · `DRAFT-storage.md` · `PLAN-storage.md`
  커밋 안 된 상태로 두는 게 맞다. **`git add -A` 로 쓸어 담지 말 것**

---

# ⚠ 절대 잊으면 안 되는 것

- **오버부킹이 1순위.** 내보내기(`exportIcal`)를 건드리면 배포 전후로
  **45개 주소(호실 9 × 채널 5) 바이트 대조**
- 수첩 오염은 **화면·통계** 문제지 오버부킹 문제가 아니다 — 내보내기는 수첩을 안 본다
- 로컬에 JS 런타임 없음 → 배포 전 검증은 verify 서버(`.verify/`, 8799)로 `worker.js` 를 브라우저에 파싱시킨다
