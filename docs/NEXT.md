# 이어서 — 지금 어디까지 했고, 다음은 뭐냐

> 사용자가 **"이어서 해"** · **"하던 거 계속"** 이라고 하면 이 파일부터 읽는다.
> 작업을 마칠 때마다 이 파일을 **덮어써서** 항상 최신 한 장만 유지한다.

**마지막 갱신**: 2026-09-24

---

## 지금 바로 할 일

### 트립닷컴 수동 기입 (`platform`)

블락에 **"이건 트립 예약이다"** 를 표시할 수 있게 만드는 일.

- 스펙 초안이 이미 있다 → [`docs/features/DRAFT-manual-trip-booking.md`](features/DRAFT-manual-trip-booking.md)
  (2026-09-17 사용자 결정. `PLAN-` 은 아직 없고 **구현 전**)
- 핵심: 기존 블락에 `platform:'tr'` 칸 하나만 추가. **워커 변경 0줄**
- 시작은 `/plan manual-trip-booking` 부터

**왜 필요한가** — 손님이 **퇴실 당일에 연장**하면 시스템이 "같은 손님 연장"과 "한 팀 나가고 새 팀
입실"을 구분하지 못한다 (날짜만으로는 입력이 완전히 동일하다, #35). 그 한 경우는 사장님이
**블락으로 직접 잡기로** 했다 (2026-09-24 결정). 그러려면 블락에 채널 표시가 있어야 한다.

---

## 직전에 끝낸 일 (2026-09-22 ~ 09-24)

| 무엇 | 커밋 | 되돌아갈 지점 |
|---|---|---|
| 원장 복구 + 피드 변화 기록 신설 | 워커 `9ea302c` | `d006986` |
| 수첩 정리 규칙 축소 + 입실일 복원을 원장으로 | 워커 `9709111` | `9ea302c` |
| 예약앱에 같은 정리 규칙 반영 | 예약앱 `c21b0d9` | `96361be` |
| 문서 #33~#35 | 문서 `f95abf3` | — |

상세는 [`05-known-issues.md`](05-known-issues.md) **#33 · #34 · #35**.

---

## 아직 안 한 것

| # | 내용 | 비고 |
|---|---|---|
| 1 | **"최신이 이긴다"** — 수첩 줄마다 '마지막 본 시각'을 적고, 충돌하면 최신을 고른다 | 변화 기록이 며칠 쌓인 뒤 실데이터로 검증하며 진행. 지금 바로 하면 **긴 차단 덩어리가 '최신'으로 이겨서** 진짜 예약을 또 덮는다 |
| 2 | **월별 결산** — 청소횟수·예약율을 달마다 못 박아 1년치 보관 | 원장이 한 달 찬 뒤 |
| 3 | **지난 화면도 원장으로** — 예약앱 과거 달력이 아직 수첩을 본다 | 원장이 한 달 찬 뒤 |
| 4 | **리브애니웨어 403** — 워커가 피드를 못 받는다 (203·401호) | 지금 예약 0건이라 피해 없음. 다만 **유일한 실제 오버부킹 경로**. 사장님이 요청할 때 진행 |
| 5 | 수첩에 남은 가짜 모양들 (`603호 10/02~10/17` 등) | 1번이 맡는다 |

---

## 다른 세션이 하고 있는 일 — 건드리지 말 것

- **비품앱** — `docs/features/DRAFT-supply-store.md` · `PLAN-supply-store.md`
  커밋 안 된 상태로 두는 게 맞다. `git add -A` 로 쓸어 담지 말 것

---

## 이 프로젝트에서 절대 잊으면 안 되는 것

- **오버부킹이 1순위.** 내보내기(`exportIcal`)를 건드리는 변경은 배포 전후로
  **45개 주소(호실 9 × 채널 5) 바이트 대조**를 한다
- 수첩(아카이브) 오염은 **화면·통계** 문제지 오버부킹 문제가 아니다. 내보내기는 수첩을 안 본다
- 로컬에 JS 런타임이 없다 → 배포 전 검증은 `.claude/launch.json` 의 **verify 서버**(`.verify/`, 8799)로
  `worker.js` 를 브라우저에 파싱시켜서 한다
